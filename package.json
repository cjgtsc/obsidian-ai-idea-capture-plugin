{
  "name": "obsidian-ai-idea-capture-plugin",
  "version": "1.0.7",
  "main": "main.js",
  "scripts": {
    "build": "mkdir -p dist && npx esbuild main.ts --bundle --minify --external:obsidian --platform=node --outfile=dist/main.js && cp manifest.json dist/ && cp styles.css dist/",
    "dev": "mkdir -p dist && npx esbuild main.ts --bundle --minify --external:obsidian --platform=node --outfile=dist/main.js && cp manifest.json dist/ && cp styles.css dist/",
    "watch": "mkdir -p dist && npx esbuild main.ts --bundle --watch --external:obsidian --platform=node --outfile=dist/main.js && cp manifest.json dist/ && cp styles.css dist/",
    "publish": "node scripts/publish.mjs"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.19.37",
    "esbuild": "^0.27.4",
    "fs-extra": "^11.3.4",
    "node-telegram-bot-api": "^0.67.0",
    "obsidian": "^1.12.3",
    "tslib": "^2.8.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "discord.js": "^14.25.1"
  }
}
